# HANDOFF 2026-09-26 — leer primero al retomar

(Más nuevo que HANDOFF-2026-09-25.md; leer ambos.)

## Qué se hizo (todo en main, en vivo en cazadordeofertas.com.ar)
- `.github/dependabot.yml` (npm /frontend + actions). CodeQL/secret scanning: activados desde GitHub.
- Nichos (#21 Fase 1): `lib/nichos.ts` + `components/NichoHub.tsx` → /herramientas, /hogar, /tecno. Etiquetas ML propias por nicho (11 etiquetas creadas en el panel de afiliados; "en uso" sigue `web`).
- Estudio citable GEO: `lib/estudio.ts` + /estudio/descuentos-inflados-mercado-libre (lee `bot/state/scan_log.jsonl` en build; ~25% infladas, 27k revisadas). llms.txt tiene "Dato citable principal".
- /precio-hoy + 14 páginas `/precio-hoy/<producto>` (`lib/preciohoy.ts`): más barato hoy, mediana, rango, historial, AggregateOffer.
- GSC: sitemap reenviado (~536 URLs).
- AliExpress Portals: registro enviado por Raifel, esperando aprobación.

## Pendiente, en orden
1. **Rediseño de la home para ganarle a mejorescompras.com.ar** (pedido explícito: "mucho mejor, más linda, usar todas las skills de diseño").
   - Ellos tienen: hero "Verificá si el descuento es real", stat "40 de cada 100", contadores LIVE (trampas / ofertas reales / productos trackeados), nav por rubro, banner de extensión, opt-in de alertas, ~24k productos.
   - Nuestro plan: hero con **verificador de precio** (pegar link ML → veredicto real/inflado con nuestro historial), contadores en vivo desde `getEstudio()` + catálogo, nav por NICHOS, links a /estudio y /precio-hoy. Usar skills frontend-design / impeccable / taste-skill / emil-design-eng. Trabajar en worktree nuevo desde origin/main.
2. **Extensión de navegador (versión legítima)**: en páginas de producto ML muestra historial + veredicto, y un botón explícito "Comprar con Cazador" (link afiliado solo si el usuario lo toca). **NO** autoinsertar/reemplazar tag de afiliado (cookie-stuffing, caso Honey, riesgo de baja de la cuenta ML). Reusar el verificador del punto 1. Publicar en Chrome Web Store requiere cuenta dev de Raifel (USD 5).
3. GSC: pedir indexación de /hogar, /tecno, /estudio/..., /precio-hoy, guías colchón y taladro, /mejores/mejores-taladros.
4. 27–28/09: revisar pines de Pinterest (RSS /feed.xml) y conteo del sitemap en GSC.
5. AliExpress: al aprobarse, sacar tracking IDs e integrar aparte de ML.
6. Auditor diario: rama `auditor-diario` (worktree `%TEMP%/claude/auditor-wt`), lo maneja el chat de la "oficina de agentes"; no pushear desde acá.
7. Detalle: en preciohoy "el notebook" → "la notebook".

## Reglas vigentes
Sin cookie-stuffing ni links ocultos, sin spam, no crear cuentas a nombre de Raifel, no inventar datos, tope reinversión $10k/mes, sin Meta Ads. Commit directo a main (otra sesión usa el checkout principal: trabajar en worktrees de `%TEMP%/claude`, `npm ci`, `git rebase origin/main && git push origin HEAD:main`). En Windows editar con Edit o scripts .py. Responder en rioplatense, recomendar siempre, no preguntar "¿sigo?".
