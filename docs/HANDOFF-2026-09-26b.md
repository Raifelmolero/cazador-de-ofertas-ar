# HANDOFF 2026-09-26b (más nuevo que HANDOFF-2026-09-26.md)

## Hecho (en main)
- Home de cazadordeofertas.com.ar (= /hoy): verificador arriba (components/Verificador.tsx) que consulta /historial.json (app/historial.json/route.ts, estático, lee bot/state/price_history.json vía lib/historial.ts). Veredicto: INFLADO si min < precio×0,95; CAZADO si precio ≤ min×1,01 con ≥3 días; si no NORMAL. Campo opcional "Precio que ves". Contadores reales, nav por NICHOS, accesos a /estudio y /precio-hoy.
- extension/ (MV3): panel con veredicto en productos de ML + botón "Comprar con Cazador" (solo on-click, etiqueta `web`). README con cómo probar/publicar.
- precio-hoy: campo `fem` y helper `art()` (la notebook, una heladera). Lint del frontend en 0 errores.

## Pendiente
1. Raifel: cuenta dev Chrome Web Store (USD 5) para publicar; crear etiqueta `extension` en panel ML y cambiar `web`→`extension` en extension/content.js.
2. GSC: pedir indexación de /hogar, /tecno, /estudio/descuentos-inflados-mercado-libre, /precio-hoy (Claude in Chrome no conectado en esta sesión).
3. 27–28/09: pines de Pinterest y conteo del sitemap en GSC.
4. Siguiente mejora de la home: ver en Clarity si la gente usa el verificador.
